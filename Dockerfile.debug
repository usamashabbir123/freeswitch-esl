# Multi-stage build for efficient final image
FROM python:3.12-slim AS builder

ENV DEBIAN_FRONTEND=noninteractive

# Debug Dockerfile intentionally disabled for production repo.
# If you need the debug Dockerfile, restore it from version control or
# create a temporary Dockerfile for local debugging. This file is kept
# in the repo as a placeholder and is not meant for building production images.
RUN apt-get update && apt-get install -y \
